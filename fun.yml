---
- name: just messing around
  hosts: localhost
  vars: 
    some_value: 
      - 2
      - 4
      - 6
      - 8

  tasks:
  - debug: 
      msg: the length of {{ some_value }} is  {{ some_value | length }}"